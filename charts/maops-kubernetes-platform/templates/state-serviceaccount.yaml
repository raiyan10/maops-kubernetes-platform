# DAY5 (unchanged design for Day 6): purpose-built identity for the
# state workload - see gateway-serviceaccount.yaml for the full
# rationale, identical here.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: maops-state
  namespace: {{ .Values.release.namespace }}
  labels:
    {{- include "maops.componentLabels" (dict "root" $ "component" "state") | nindent 4 }}
automountServiceAccountToken: false
